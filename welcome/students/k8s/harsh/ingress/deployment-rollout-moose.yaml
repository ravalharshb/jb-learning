apiVersion: apps/v1
kind: Deployment
metadata:
  name: animal-moose-deployment-rollout
spec:
  strategy:
    rollingUpdate:
      maxSurge: 100%
      maxUnavailable: 0%
    type: RollingUpdate
  selector:
    matchLabels:
      app: moose-deploy
  replicas: 2 
  template: 
    metadata:
      labels:
        app: moose-deploy
    spec:
      containers:
      - name: animal-moose
        image: supergiantkir/animals:moose
        ports:
        - containerPort: 80
